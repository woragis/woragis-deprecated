# Troubleshooting & FAQ

## Common Issues

- Missing environment variables: check `.env` and config docs
- LLM/API errors: check logs and error codes
- Browser crashes: check Playwright version and logs

## Debugging

- Use `run_id` and `job_id` to trace issues
- Check logs, WebSocket events, and DB records for context
